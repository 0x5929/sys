# this command below will echo the filename and cat the contents below, for regular and hidden files in a directory

for file in $(find . -maxdepth 1 -type f); do echo -e "\nfilename: $file\n"; cat $file; done
